// function convertToMarkdown(element) {
//     // TODO: Convert the element's content to Markdown
//     return markdown;
// }

// let markdown = convertToMarkdown(document.querySelector('selector-for-qa-content'));

// // Copy the Markdown to the clipboard
// navigator.clipboard.writeText(markdown);
